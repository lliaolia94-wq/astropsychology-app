-- ============================================================
-- Скрипт для добавления поля birth_time_utc_offset в таблицу users
-- Используйте этот скрипт, если миграция Alembic не работает
-- ============================================================

-- ВАЖНО: Выберите команду для вашей базы данных!
-- Раскомментируйте (уберите --) только одну команду ниже

-- ============================================================
-- ДЛЯ SQLITE (по умолчанию в проекте) - РАСКОММЕНТИРУЙТЕ ЭТУ СТРОКУ:
-- ============================================================
ALTER TABLE users ADD COLUMN birth_time_utc_offset DECIMAL(5, 2);

-- ============================================================
-- ДЛЯ POSTGRESQL - РАСКОММЕНТИРУЙТЕ ЭТУ СТРОКУ:
-- ============================================================
-- ALTER TABLE users ADD COLUMN IF NOT EXISTS birth_time_utc_offset DECIMAL(5, 2);

-- ============================================================
-- ДЛЯ MYSQL - РАСКОММЕНТИРУЙТЕ ЭТУ СТРОКУ:
-- ============================================================
-- ALTER TABLE users ADD COLUMN birth_time_utc_offset DECIMAL(5, 2) NULL;

-- ============================================================
-- ПРОВЕРКА (после выполнения команды выше):
-- ============================================================

-- Для SQLite:
-- PRAGMA table_info(users);

-- Для PostgreSQL:
-- SELECT column_name, data_type 
-- FROM information_schema.columns 
-- WHERE table_name = 'users' AND column_name = 'birth_time_utc_offset';

-- Для MySQL:
-- DESCRIBE users;

