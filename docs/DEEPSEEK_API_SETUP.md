# Настройка DeepSeek API

> 💡 **Для бесплатного тестирования** используйте Ollama (локальная модель) - см. `docs/README_FREE_AI_SETUP.md`

## Получение API ключа DeepSeek

### Шаг 1: Регистрация на DeepSeek

1. Перейдите на официальный сайт DeepSeek: **https://platform.deepseek.com/**
2. Нажмите "Sign Up" или "Регистрация"
3. Создайте аккаунт (можно использовать email или другие доступные способы)

### Шаг 2: Получение API ключа

1. После входа в личный кабинет, перейдите в раздел **"API Keys"** или **"API"**
2. Нажмите кнопку **"Create new API key"** или **"Создать новый API ключ"**
3. Введите название для ключа (например: "Astropsychology App")
4. Подтвердите создание
5. **ВАЖНО**: Скопируйте ключ сразу! Он показывается только один раз и выглядит примерно так: `sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

### Шаг 3: Настройка в проекте

1. Создайте или откройте файл `.env` в корне проекта
2. Добавьте строку:
   ```
   DEEPSEEK_API_KEY=sk-ваш_ключ_здесь
   ```
3. Сохраните файл

### Шаг 4: Проверка

После добавления ключа перезапустите сервер и попробуйте отправить запрос к `/ai/chat`.

## Решение проблем с API

### Ошибка 402: Недостаточно баланса

Если вы получаете ошибку **402 "Insufficient Balance"**, это означает, что на вашем аккаунте DeepSeek недостаточно средств.

**Решение:**
1. Зайдите на https://platform.deepseek.com/
2. Пополните баланс аккаунта
3. Проверьте лимиты использования API

### Ошибка 404: Ресурс не найден

Если вы получаете ошибку **404**, это обычно означает:

1. **Неверное имя модели** - используйте только актуальные модели
2. **Изменения в API** - DeepSeek мог обновить доступные модели
3. **Проблемы с доступом** - API ключ может не иметь доступа к модели

**Решение:**

1. **Используйте актуальные модели** - согласно официальной документации DeepSeek-V3.2-Exp:
   - `deepseek-chat` - режим без размышлений (быстрее)
   - `deepseek-reasoner` - режим с размышлениями (более точный)
   
   Добавьте в `.env` файл:
   ```env
   DEEPSEEK_MODEL=deepseek-chat
   ```
   или
   ```env
   DEEPSEEK_MODEL=deepseek-reasoner
   ```

2. **Проверьте доступные модели** - зайдите в личный кабинет DeepSeek: https://platform.deepseek.com/

3. **Запустите тестовый скрипт** - он автоматически проверит доступные модели:
   ```bash
   python scripts/test_deepseek_key.py
   ```

4. **Проверьте URL endpoint** - убедитесь, что в `.env` указан правильный URL:
   ```env
   DEEPSEEK_API_URL=https://api.deepseek.com/v1/chat/completions
   ```

### Ошибка 400: Model Not Exist

Если вы получаете ошибку **400 "Model Not Exist"**, это означает, что указанная модель не существует.

**Решение:**
- Используйте только актуальные модели: `deepseek-chat` или `deepseek-reasoner`
- НЕ используйте устаревшие модели типа `deepseek-chat-v2` или `deepseek-chat-v3`

## Дополнительная информация

- **Документация API**: https://api-docs.deepseek.com/
- **Актуальные модели DeepSeek-V3.2-Exp**:
  - `deepseek-chat` - режим без размышлений (быстрее, по умолчанию)
  - `deepseek-reasoner` - режим с размышлениями (более точный, медленнее)
- **Баланс**: DeepSeek требует пополнения баланса для использования API (бесплатный тариф может быть ограничен)

## Пример файла .env

```env
# DeepSeek API
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# База данных (если нужно)
DATABASE_URL=postgresql://user:password@localhost/dbname

# Другие настройки
# Актуальные модели: deepseek-chat (быстрее) или deepseek-reasoner (точнее)
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_MAX_RETRIES=3
DEEPSEEK_TIMEOUT=60
```

## Безопасность

⚠️ **ВАЖНО**: 
- Никогда не коммитьте файл `.env` в git
- Не делитесь API ключом публично
- Файл `.env` уже добавлен в `.gitignore`

## Альтернативные способы получения ключа

Если у вас возникли проблемы с получением ключа напрямую, можно использовать:
- Официальный сайт DeepSeek: https://platform.deepseek.com/
- Документацию: https://api-docs.deepseek.com/

