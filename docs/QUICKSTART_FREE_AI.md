# 🚀 Быстрый старт с бесплатной AI моделью

## Шаг 1: Установите Ollama

### Windows
1. Скачайте: https://ollama.com/download
2. Запустите установщик
3. Ollama автоматически запустится

### macOS / Linux
```bash
curl https://ollama.com/install.sh | sh
```

## Шаг 2: Загрузите модель

Откройте терминал и выполните:

```bash
ollama pull llama3.2
```

Это займет несколько минут (модель ~2GB). После загрузки модель будет готова к использованию.

## Шаг 3: Настройте проект

Добавьте в файл `.env` в корне проекта:

```env
# Использовать бесплатную локальную модель Ollama
AI_PROVIDER=ollama
OLLAMA_MODEL=llama3.2
```

## Шаг 4: Перезапустите приложение

Перезапустите ваш сервер - теперь приложение будет использовать бесплатную локальную модель!

## Готово! ✅

Теперь вы можете тестировать AI-чат без необходимости пополнения баланса.

## Проверка работы

1. Убедитесь, что Ollama запущена:
   ```bash
   curl http://localhost:11434/api/tags
   ```
   Должен вернуть список моделей, включая `llama3.2`

2. Отправьте запрос к `/ai/chat` - должно работать!

## Дополнительные модели

Если хотите попробовать другие модели:

```bash
ollama pull llama3.1      # Более крупная модель (~4GB)
ollama pull mistral       # Mistral 7B (~4GB)
ollama pull qwen2.5       # Qwen 2.5 (~4GB)
```

Затем укажите в `.env`:
```env
OLLAMA_MODEL=llama3.1  # или другая модель
```

## Проблемы?

См. подробную инструкцию: `docs/README_FREE_AI_SETUP.md`

