# 🆓 Настройка бесплатной AI модели для тестирования

Для тестирования приложения без необходимости пополнения баланса DeepSeek, вы можете использовать **Ollama** - локальную бесплатную модель ИИ.

## Что такое Ollama?

Ollama - это инструмент для запуска больших языковых моделей (LLM) локально на вашем компьютере. Это полностью бесплатно и не требует интернет-соединения после установки.

## Установка Ollama

### Windows

1. Скачайте установщик с официального сайта: https://ollama.com/download
2. Запустите установщик и следуйте инструкциям
3. Ollama автоматически установится и запустится как служба

### macOS / Linux

```bash
curl https://ollama.com/install.sh | sh
```

## Запуск модели

После установки Ollama, загрузите одну из бесплатных моделей:

```bash
# Популярные модели (выберите одну):
ollama pull llama3.2          # Meta Llama 3.2 (рекомендуется, ~2GB)
ollama pull llama3.1          # Meta Llama 3.1 (~4GB)
ollama pull mistral           # Mistral 7B (~4GB)
ollama pull qwen2.5           # Qwen 2.5 (~4GB)
ollama pull gemma2            # Google Gemma 2 (~2GB)
```

**Рекомендация**: Для начала используйте `llama3.2` - это хороший баланс между качеством и размером.

## Настройка в проекте

Добавьте в файл `.env` в корне проекта:

```env
# Использовать Ollama (бесплатно, локально)
AI_PROVIDER=ollama

# URL Ollama (по умолчанию: http://localhost:11434)
OLLAMA_API_URL=http://localhost:11434/v1/chat/completions

# Модель Ollama (должна быть загружена через ollama pull)
OLLAMA_MODEL=llama3.2
```

## Проверка работы

1. Убедитесь, что Ollama запущена (должна работать автоматически после установки)
2. Проверьте доступность API:
   ```bash
   curl http://localhost:11434/api/tags
   ```
   Должен вернуть список загруженных моделей

3. Перезапустите ваше приложение

4. Попробуйте отправить запрос к `/ai/chat` - теперь будет использоваться бесплатная локальная модель!

## Преимущества Ollama

✅ **Полностью бесплатно** - нет ограничений на использование  
✅ **Работает офлайн** - не требует интернет после установки  
✅ **Приватность** - все данные остаются на вашем компьютере  
✅ **Быстро** - работает локально, без задержек сети  
✅ **Неограниченное использование** - никаких лимитов  

## Недостатки

⚠️ **Требует ресурсы** - модели занимают место на диске (2-4GB)  
⚠️ **Требует RAM** - модели используют оперативную память (4-8GB рекомендуется)  
⚠️ **Медленнее облачных** - скорость зависит от вашего компьютера  

## Альтернативные бесплатные варианты

### 1. Mistral API (бесплатный тариф)

```env
AI_PROVIDER=mistral
MISTRAL_API_KEY=ваш_ключ
AI_API_URL=https://api.mistral.ai/v1/chat/completions
AI_MODEL=mistral-small
```

### 2. LM Studio (локально)

LM Studio - еще одна альтернатива для локального запуска моделей:
- Скачайте: https://lmstudio.ai/
- Настройте API на порту 1234
- В `.env` укажите:
  ```env
  AI_PROVIDER=lmstudio
  AI_API_URL=http://localhost:1234/v1/chat/completions
  AI_MODEL=название_модели
  ```

## Переключение между провайдерами

Вы можете легко переключаться между провайдерами, изменяя `AI_PROVIDER` в `.env`:

```env
# Для бесплатного локального использования
AI_PROVIDER=ollama

# Для DeepSeek (требует баланс)
AI_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-...

# Для других OpenAI-совместимых API
AI_PROVIDER=custom
AI_API_URL=https://...
AI_MODEL=...
```

## Устранение проблем

### Ollama не запускается

1. Проверьте, что Ollama установлена: `ollama --version`
2. Запустите вручную: `ollama serve`
3. Проверьте, что порт 11434 свободен

### Модель не найдена

```bash
# Проверьте загруженные модели
ollama list

# Загрузите модель, если её нет
ollama pull llama3.2
```

### Ошибка подключения

1. Убедитесь, что Ollama запущена: `curl http://localhost:11434/api/tags`
2. Проверьте URL в `.env`: `OLLAMA_API_URL=http://localhost:11434/v1/chat/completions`
3. Проверьте firewall - порт 11434 должен быть доступен

## Дополнительная информация

- **Официальный сайт Ollama**: https://ollama.com/
- **Документация**: https://github.com/ollama/ollama
- **Доступные модели**: https://ollama.com/library

