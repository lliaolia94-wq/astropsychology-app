# Веб-интерфейс для Астопсихологии

## Описание

Веб-интерфейс позволяет пользователям рассчитать натальную карту и получить AI-интерпретацию **без регистрации**. Это упрощает вход для новых пользователей и делает сервис доступным для всех.

## Функциональность

### ✨ Основные возможности

1. **Расчет натальной карты без регистрации**
   - Ввод даты, времени и места рождения
   - Автоматическое геокодирование города
   - Расчет профессиональной натальной карты

2. **Отображение результатов**
   - Главные знаки (Солнце, Луна, Асцендент)
   - Позиции всех планет в знаках и домах
   - Визуальное представление карты

3. **AI-интерпретация**
   - Персонализированный анализ карты
   - Использование шаблона "natal_analysis"
   - Красивое форматирование ответа

## Установка и запуск

### 1. Структура файлов

После создания веб-интерфейса, структура проекта выглядит так:

```
astropsychology-app/
├── static/
│   ├── index.html      # Главная страница
│   ├── style.css       # Стили
│   └── app.js          # JavaScript логика
├── routers/
│   └── guest.py        # API для гостевых расчетов
└── main.py             # FastAPI приложение
```

### 2. Запуск сервера

```bash
python main.py
```

Или через uvicorn:

```bash
uvicorn main:app --reload
```

### 3. Открытие веб-интерфейса

Откройте в браузере:
```
http://localhost:8000/
```

## API Endpoints

### Гостевые расчеты (без авторизации)

#### POST `/api/guest/calculate-chart`

Рассчитывает натальную карту без регистрации.

**Request:**
```json
{
  "name": "Гость",
  "birth_date": "1990-10-17",
  "birth_time": "14:30",
  "birth_place": "Москва",
  "birth_country": "Россия",
  "houses_system": "placidus"
}
```

**Response:**
```json
{
  "success": true,
  "chart_data": {
    "planets": {...},
    "aspects": [...],
    "houses": {...},
    "angles": {...}
  },
  "main_signs": {
    "sun": "libra",
    "moon": "scorpio",
    "ascendant": "gemini"
  }
}
```

#### POST `/api/guest/ai-interpretation`

Получает AI-интерпретацию карты.

**Request:**
```json
{
  "chart_data": {...},
  "question": "Расскажи о моей карте",
  "template_type": "natal_analysis"
}
```

**Response:**
```json
{
  "success": true,
  "interpretation": "💫 Приветствие...",
  "template_type": "natal_analysis"
}
```

## Особенности реализации

### Гостевой режим

- **Не сохраняет данные в БД** - расчеты выполняются "на лету"
- **Не требует регистрации** - пользователь может сразу использовать сервис
- **Использует существующие сервисы** - `astro_service`, `geocoding_service`, `ai_service`

### Безопасность

- CORS настроен для работы с фронтендом
- Rate limiting можно добавить через middleware
- Валидация входных данных через Pydantic

### UX улучшения

- Красивый современный дизайн
- Адаптивная верстка (mobile-friendly)
- Индикаторы загрузки
- Обработка ошибок с понятными сообщениями
- Автодополнение городов (базовая реализация)

## Сравнение с NatalGPT.ru

### Преимущества нашего решения:

✅ **Больше функций** - синастрия, транзиты, календари (в разработке)
✅ **10 типов AI-шаблонов** против одного чата
✅ **Сохранение истории** (для зарегистрированных пользователей)
✅ **Профессиональные алгоритмы** - Swiss Ephemeris

### Что можно улучшить:

- [ ] Добавить монетизацию (звезды, рефералы)
- [ ] Улучшить автодополнение городов
- [ ] Добавить визуализацию карты (график)
- [ ] Добавить примеры интерпретаций
- [ ] SEO-оптимизация

## Разработка

### Добавление новых функций

1. **Новые шаблоны AI** - уже поддерживаются через `ai_service`
2. **Визуализация карты** - можно добавить библиотеку для отрисовки
3. **Экспорт результатов** - PDF, изображение карты

### Кастомизация дизайна

Измените файлы в `static/`:
- `style.css` - стили и цвета
- `index.html` - структура страницы
- `app.js` - логика взаимодействия

## Тестирование

### Вручную:

1. Откройте `http://localhost:8000/`
2. Заполните форму:
   - Дата: `1990-10-17`
   - Время: `14:30`
   - Город: `Москва`
3. Нажмите "Получить карту"
4. Проверьте отображение результатов
5. Нажмите "Получить AI-интерпретацию"

### Через API:

```bash
curl -X POST "http://localhost:8000/api/guest/calculate-chart" \
  -H "Content-Type: application/json" \
  -d '{
    "birth_date": "1990-10-17",
    "birth_time": "14:30",
    "birth_place": "Москва"
  }'
```

## Troubleshooting

### Проблема: "Город не найден"

**Решение:** Убедитесь, что:
- Город есть в базе данных (`data/cities_db.json`)
- Используется правильное название (например, "Москва", а не "МСК")
- Указана страна, если город не уникален

### Проблема: "Ошибка расчета карты"

**Решение:** Проверьте:
- Формат даты (YYYY-MM-DD или DD.MM.YYYY)
- Формат времени (HH:MM)
- Swiss Ephemeris установлен и работает

### Проблема: Веб-интерфейс не открывается

**Решение:**
1. Проверьте, что папка `static/` существует
2. Убедитесь, что файлы `index.html`, `style.css`, `app.js` на месте
3. Проверьте логи сервера на ошибки

## Следующие шаги

1. ✅ Веб-интерфейс создан
2. ✅ Гостевой режим работает
3. ⏳ Добавить монетизацию
4. ⏳ Улучшить UX (автодополнение, визуализация)
5. ⏳ SEO и маркетинг

---

**Вопросы?** Откройте issue или обратитесь к разработчикам.

